@model IEnumerable<TH04_CNPMNC.Models.ChiTietSach>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>ChiTietSach</h2>

<p>
    @Html.ActionLink("Tao Chi Tiet Sach", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TheLoai)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Sach.TenSach)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TheLoai)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Sach.TenSach)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaCTS }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaCTS }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaCTS })
        </td>
    </tr>
}

</table>
